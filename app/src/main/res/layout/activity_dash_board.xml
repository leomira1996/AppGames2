<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_dash_board"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="16dp"
    android:paddingLeft="64dp"
    android:paddingRight="64dp"
    android:paddingTop="16dp"
    tools:context="com.example.miranda.appgames.Activity.DashBoardActivity">

    <Button
        android:id="@+id/inserirSerieButton"
        android:onClick="inserirGame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cadastrar Game" />

    <Button
        android:layout_below="@+id/inserirSerieButton"
        android:id="@+id/listarSerieButton"
        android:onClick="listarGame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Listar" />

    <Button
        android:layout_below="@+id/listarSerieButton"
        android:id="@+id/inserirCategoriaButton"
        android:onClick="inserirCategoria"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Nova Categoria" />
</RelativeLayout>
